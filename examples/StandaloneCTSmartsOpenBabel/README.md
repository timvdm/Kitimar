# Build

```shell
$ cd StandaloneCTSmartsOpenBabel
$ mkdir build
$ cd build
$ cmake -DCMAKE_BUILD_TYPE=Release -DKitimar_DIR=/path/to/lib/cmake/Kitimar -DOpenBabel3_DIR=/path/to/lib/cmake/openbabel3 ..
```
